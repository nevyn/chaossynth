# synth

The SuperCollider engine — runs identically on a dev Mac and the Pi. Not built
yet — the brief is [docs/plans/synth-engine.md](../docs/plans/synth-engine.md).

mapping.json is live and seeded from the current firmware tables; its schema is
pinned in [docs/midi-contract.md](../docs/midi-contract.md).
